#!/bin/sh
root=$(git rev-parse --show-toplevel) || exit 0
if [ -e "$root/ai-parity/.sync-journal.json" ]; then
  echo "ai-parity: recover the active transaction before completing a merge" >&2
  exit 1
fi
exit 0
