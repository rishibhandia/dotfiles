# Targets chezmoi should delete from machines (target paths, like .chezmoiignore)
#
# Entries are temporary: once every machine has applied past a cleanup, delete
# the entry (the 2026-06-11 batch was removed 2026-08 after all machines
# converged). Gotcha kept for reference: an exact .chezmoiignore match
# suppresses the corresponding remove entry (children of an ignored dir still
# work), which is why ~/README.md, ~/install.sh, and ~/install.ps1 once had to
# be deleted by hand per machine.

# Zed dropped 2026-08 (cask + config removed from source); clean up the
# settings dir still deployed on other machines.
.config/zed
.config/zed/settings.json
