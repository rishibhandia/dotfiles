# Brewfile - Homebrew packages for dotfiles
# Install with: brew bundle --file=~/.Brewfile
# Dump current packages: brew bundle dump --file=~/.Brewfile --force --describe

# =============================================================================
# CLI Tools - Essential
# =============================================================================
brew "chezmoi"                # Dotfiles manager
brew "git"                    # Version control
brew "zsh"                    # Shell
brew "starship"               # Cross-shell prompt
brew "neovim"                 # Text editor
brew "tmux"                   # Terminal multiplexer
brew "mosh"                   # SSH replacement for unreliable links (with tmux for remote sessions)

# =============================================================================
# CLI Tools - Modern replacements
# =============================================================================
brew "bat"                    # Better cat
brew "lsd"                    # Better ls
brew "fd"                     # Better find
brew "ripgrep"                # Better grep
brew "btop"                   # Better top
brew "fastfetch"              # System info display
brew "fzf"                    # Fuzzy finder
brew "zoxide"                 # Better cd (z command)
brew "duf"                    # Better df (disk usage)

# =============================================================================
# CLI Tools - Utilities
# =============================================================================
{{ if eq .chezmoi.os "darwin" -}}
brew "duti"                   # Set default applications (macOS-only formula)
{{ end -}}
brew "tree"                   # Directory listing
brew "curl"                   # HTTP client
brew "wget"                   # HTTP client
brew "jq"                     # JSON processor
brew "yq"                     # YAML processor
brew "rclone"                 # Cloud storage sync
brew "navi"                   # Interactive cheatsheet
brew "yt-dlp"                 # Video downloader
brew "tesseract"              # OCR engine
brew "age"                    # File encryption (used by chezmoi for encrypted source files)

# =============================================================================
# CLI Tools - Archive/Compression
# =============================================================================
brew "p7zip"                  # 7-Zip
brew "unar"                   # Universal archive extractor (handles RAR too)
brew "pigz"                   # Parallel gzip

# =============================================================================
# Development - Languages & Runtimes
# =============================================================================
brew "uv"                     # Fast Python package manager & CLI tool runner
brew "node"                   # Node.js
brew "go"                     # Go language
brew "rustup"                 # Rust toolchain manager

# =============================================================================
# Development - Tools
# =============================================================================
brew "gh"                     # GitHub CLI
brew "ruff"                   # Fast Python linter
brew "shellcheck"             # Shell script linter
brew "shfmt"                  # Shell script formatter

# Containers (Colima + Docker) moved into the Mac-mini-only block below.

# =============================================================================
# AI/LLM Tools
# =============================================================================
tap "sheeki03/tap"
brew "sheeki03/tap/tirith"    # Terminal security tool (URL/ANSI injection guard)
brew "rtk"                    # Claude Code output compressor (60-90% token reduction)
brew "llm"                    # LLM CLI (required by gcm/q/qv/sheet2csv/pdf2text functions)

# =============================================================================
# Zsh plugins
# =============================================================================
brew "zsh-syntax-highlighting" # Fish-like syntax highlighting
brew "zsh-autosuggestions"     # Fish-like autosuggestions
brew "zsh-completions"         # Additional zsh completions

{{ if eq .chezmoi.os "darwin" -}}
# =============================================================================
# macOS Casks - Terminal
# =============================================================================
cask "ghostty"                # GPU-accelerated terminal

# =============================================================================
# macOS Casks - Development
# =============================================================================
cask "zed"                    # Code editor
cask "coteditor"              # Plain-text editor (default $EDITOR via bundled `cot -w` CLI)

# =============================================================================
# macOS Casks - Productivity
# =============================================================================
cask "skim"                   # PDF reader
cask "zotero"                 # Reference manager (browser connector: install from zotero.org/download/connectors)
cask "inkscape"               # Vector editor (figure editing; no self-updater, so brew upgrades it)

# =============================================================================
# macOS Casks - System maintenance
# =============================================================================
cask "onyx"                   # macOS maintenance/tuning utility (cleanup, verify, tweaks)

{{ if not .work -}}
# =============================================================================
# Personal-only apps - mirrors the Scoopfile's work-machine exclusions
# (personal accounts and mesh networking stay off corp machines)
# =============================================================================
cask "1password"              # Password manager
cask "1password-cli"          # 1Password CLI
cask "logseq"                 # Note taking / PKM
cask "iina"                   # macOS-native mpv GUI: styled ASS subs + ICC colour
cask "vlc"                    # Universal fallback: odd codecs, DVD/Blu-ray, streams
cask "tailscale-app"          # VPN/mesh network (security risk on corp networks)
cask "ollama-app"             # Local LLM runtime
{{ end -}}

# =============================================================================
# Fonts
# =============================================================================
cask "font-hack-nerd-font"        # Hack font with icons
cask "font-meslo-lg-nerd-font"    # Meslo font for terminal

# =============================================================================
# (Mac App Store / mas removed — broken on modern macOS, no App Store apps declared)
{{- end }}

{{ if .personal -}}
# =============================================================================
# Personal-only tools (gated by .personal in chezmoi data)
# =============================================================================
tap "run-llama/liteparse"
brew "run-llama/liteparse/llamaindex-liteparse"   # Local PDF parser (LlamaIndex, runs offline)
{{- end }}

{{ if and (eq .chezmoi.os "darwin") (eq .hostname "rishi-macmini-2020") -}}
# =============================================================================
# Mac mini only - always-on LAN services
# =============================================================================
cask "jellyfin"               # Self-hosted media server (LAN-wide)
# Containers — Colima + Docker CLI (replaces Docker Desktop). Hosts the Shoko
# metadata backend for Jellyfin/Shokofin as a Docker container via Colima
# (see run_after_07-install-shoko). Mac-mini-only; not needed on laptops.
brew "colima"                 # Container runtime (Linux VM via Lima, FOSS)
brew "docker"                 # Docker CLI (daemon provided by Colima)
brew "docker-compose"         # Compose v2 plugin
brew "fswatch"                # Watches the media drive -> triggers Shoko auto-import (run_after_09)
{{- end }}
